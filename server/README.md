# ElevateU API

Laravel 12.69.2 / Passport 12.4.3 / MySQL API with Reverb 1.11.1 and a database queue. Docker pins PHP 8.2.33, Composer 2.10.3 and nginx 1.30.5. Disposable tests now use MySQL 8.4.11; existing development retains 8.0.46 until an explicit cutover. Day 4 verifies synthetic logical backup/restore across those engines; it does not upgrade normal data. Follow the [development workflow](../docs/DEVELOPMENT.md) and [database runbook](../docs/DATABASE.md). Preserve existing `.env` files and databases; never use a reset as a setup or upgrade shortcut.

PHPUnit must run in the documented network-disabled SQLite runtime, separate from the disposable HTTP/WebSocket Compose stack. The test bootstrap fails closed if the resolved database is not SQLite memory. Day 4 passes 52 tests / 489 assertions and PHP syntax checks. Scoped Pint passes all 22 materially changed PHP files; full Pint retains 46 legacy failures (previously 49, with three rewritten fixture files now clean). MySQL checks live separately in `tests/MySql`; they explicitly report the unresolved conversation/membership races. Current live integration results belong in the [security report](../docs/SECURITY.md). Do not mass-format the repository.

`php artisan elevateu:db-preflight` reports 46 read-only integrity checks, returning nonzero for blocking violations. Default seeding is empty; the dedicated `DemoSeeder` requires an isolated allowlisted local/testing database, explicit enablement and a supplied password. [Fixture commands and safety rules](../docs/DATABASE.md#factories-and-demo-fixtures) include running Docker seeding as `www-data` to preserve upload-directory permissions.

Day 3 passed clean Composer installation and the server Docker build. Full and production-only Composer audits reported zero findings. The separate Laravel Vite bundle moved to Vite 6.4.3, passed an isolated `npm ci` and build, and has zero findings in both npm audit modes. These checks do not replace authentication, private-media and live Reverb regression testing.

See [architecture](../docs/ARCHITECTURE.md), [API contracts](../docs/API.md), [security decisions and legacy private-media rollout](../docs/SECURITY.md), and [dependency findings](../docs/DEPENDENCIES.md). OAuth routes are explicitly unavailable until their safe replacement is verified.
